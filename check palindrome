#include <stdio.h>
int main() {
    int n=121, t=n, r=0;
    while(n!=0){
        r = r*10 + n%10;
        n/=10;
    }
    if(t==r)
        printf("Palindrome");
    else
        printf("Not Palindrome");
    return 0;
}
